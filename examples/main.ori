/*
    Example main function. This serves as the entrypoint to an Orion
    program, and it's return value should be the exit code of the program.
*/
func main() : usize = {
    let a : u32 = 2;
    let b32 : i32 = 12;

    return b32 + a;
}
